def count_letters(text):
    """
        Подсчитывает количество каждой буквы в тексте.

        Аргументы:
            text: строка с текстом

        Возвращает:
            словарь, где ключ — буква (в нижнем регистре), значение — количество
        """
    # Создаём пустой словарь для хранения количества каждой буквы
    letter_count = {}
    # Проходим по каждому символу в тексте
    for char in text:
        # Метод isalpha() проверяет, является ли символ буквой
        # Если это не буква (пробел, запятая, точка и т.д.), пропускает
        if char.isalpha():
            # Метод lower() приводит букву к нижнему регистру
            # Это нужно, чтобы 'А' и 'а' считались одинаково
            letter = char.lower()
            # Проверяем, есть ли уже такая буква в словаре
            # Если буква уже есть, увеличиваем счётчик на 1
            if letter in letter_count:
                letter_count[letter] += 1

            else:
                # Если буквы ещё нет, добавляем её со значением 1
                letter_count[letter] = 1
    # Возвращаем словарь с подсчитанными буквами
    return letter_count


def calculate_frequency(letter_count):
    """
    Вычисляет частоту каждой буквы.

    Аргументы:
        letter_count: словарь с количеством каждой буквы

    Возвращает:
        словарь, где ключ — буква, значение — частота (доля от общего количества)
    """
    # Метод values() возвращает все значения словаря
    # sum() суммирует их — получаем общее количество букв в тексте
    total = sum(letter_count.values())
    # Создаём пустой словарь для хранения частот
    frequency = {}
    # Метод items() возвращает пары (ключ, значение) для удобной итерации
    # Проходим по каждой букве и её количеству
    for letter, count in letter_count.items():
        # Вычисляем частоту: количество буквы / общее количество всех букв
        frequency[letter] = count / total
    # Возвращаем словарь с частотами
    return frequency

# Основная программа
main_str = """
У лукоморья дуб зелёный;
Златая цепь на дубе том:
И днём и ночью кот учёный
Всё ходит по цепи кругом;
Идёт направо — песнь заводит,
Налево — сказку говорит.
Там чудеса: там леший бродит,
Русалка на ветвях сидит;
Там на неведомых дорожках
Следы невиданных зверей;
Избушка там на курьих ножках
Стоит без окон, без дверей;
Там лес и дол видений полны;
Там о заре прихлынут волны
На брег песчаный и пустой,
И тридцать витязей прекрасных
Чредой из вод выходят ясных,
И с ними дядька их морской;
Там королевич мимоходом
Пленяет грозного царя;
Там в облаках перед народом
Через леса, через моря
Колдун несёт богатыря;
В темнице там царевна тужит,
А бурый волк ей верно служит;
Там ступа с Бабою Ягой
Идёт, бредёт сама собой,
Там царь Кащей над златом чахнет;
Там русский дух… там Русью пахнет!
И там я был, и мёд я пил;
У моря видел дуб зелёный;
Под ним сидел, и кот учёный
Свои мне сказки говорил.
"""

# Выполняем анализ
# Шаг 1: подсчитываем количество каждой буквы
letter_counts = count_letters(main_str)
# Шаг 2: вычисляем частоту каждой буквы
frequencies = calculate_frequency(letter_counts)

# Шаг 3: выводим результат в столбик
# Проходим по всем парам (буква, частота) в словаре
for letter, freq in frequencies.items():
    # Шаг 3: выводим результат в столбик
    # Проходим по всем парам (буква, частота) в словаре
    print(f"{letter}: {freq:.2f}")